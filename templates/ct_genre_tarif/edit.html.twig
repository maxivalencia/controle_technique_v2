{% extends 'base.html.twig' %}

{% block title %}Edition Tarif Genre{% endblock %}

{% block body %}
    <h1>Edition Tarif Genre</h1>

    {{ include('ct_genre_tarif/_form.html.twig', {'button_label': 'Modifier'}) }}
            </td>
            <td style="width:150px;">
    <a href="{{ path('app_ct_genre_tarif_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour</a>
            </td>
            <td style="width:150px;">
    {{ include('ct_genre_tarif/_delete_form.html.twig') }}
            </td>
        </tr>
    </tbody>
</table>
{% endblock %}